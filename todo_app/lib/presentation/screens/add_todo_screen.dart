import 'package:flutter/material.dart';

class AddTodoScren extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add Todo"),
      ),
    );
  }
}
